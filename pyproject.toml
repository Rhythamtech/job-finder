[project]
name = "jobfinder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=1.0.5",
    "langgraph-checkpoint>=3.0.1",
    "openai>=2.14.0",
    "python-dotenv>=1.2.1",
    "redis>=7.1.0",
]
